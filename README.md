# eer-diagram-designer — moved

This project is now **[DBMS Modeling Tool](https://github.com/brian7952002/dbms-modeling-tool)**.

This repository exists only to keep links that were handed out under the old address working.
GitHub does not redirect old GitHub Pages project URLs after a rename, and the shared diagrams live
in the URL fragment — `#c=` a published diagram, `#d=` a whole diagram packed into the link,
`#join=` a project invite. The page here forwards to the new address preserving that fragment.

**Do not delete this repository.** It is the only thing keeping those links alive.
